 via 10.1.0.1 }

# Site B
node rch with a router and a .0.0/24 via 172.16.0.a 11.2fault via 10.2.0. each with a rout2r and a host, connected
# by_ a WAN 2link with 

# WAN link with symmet a rout2r and a host, connected
# by_ a WAN 2link with 

# WAN link with symmetric impairment
link router-a:wan02.0. each with a rout2r and a host, connected
# by_ a WAN 2link with 

# WAN link with symric impairment
link router-a:wan02.0. each with a rout2r and a host, connected
# by_ a WAN 2link with 

# WAN link with symmetric %mpairment
 rate 50mbit
}
