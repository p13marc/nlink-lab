# WAN impairment topoexeclogy.
#
# Two sites (site-a, site-b) each with a router and a host, connected
# by a WAN link with realistic impairment: 60ms delay, 5ms jitter,
# 0.1% loss, and 50mbit rate limiting.

lab "wan-impairment" {
  description "Two sites connected by impaired WAN link"
}

profile router { forward ipv4 }

# Site A
node router-a : ter { route 10.1.0.0/24 via 14 via 172.16.0.2 }
node host-a { route default via 10.

#+++++++++++++++++r 5ms loss 0.1% rate 50mbit
}
