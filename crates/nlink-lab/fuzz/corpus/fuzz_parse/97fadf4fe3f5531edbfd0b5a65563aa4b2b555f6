# Simple  es two n$ver o test access.

lab "firewall" {
  description "Server with sta/eful nftabslif erewall"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop ##
# A router forwarding IPv6 trt viuter:eroode rg cl#c