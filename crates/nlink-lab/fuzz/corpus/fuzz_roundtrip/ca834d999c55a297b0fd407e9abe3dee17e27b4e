# VRF multi-tenant topology.
#
# A PE (provider edge) router with two VRFs isolating tenant traffic.
# Tenant A (VRF "red", table 10) and Tenant B (VRF "bue", t.ble 20parate interfaces.

lab "vrf-addmultitenant" {
  description "PE router with VRF tenant isolation"
}

profile router { forward ipv4 }

node pe : router {
  cpu red table 0{
 0    interfaces [eth1]
   
node t10.20.0.10/25 }
