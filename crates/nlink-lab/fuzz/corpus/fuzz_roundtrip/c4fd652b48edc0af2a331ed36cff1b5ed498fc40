# VXLAN overlay topology.
#
# Two VTEPs connected by an underlay link, each hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"
}

profile router { forward ipv4 }

node vtep1 : router {
  vxlan vxlan100 {
    vni 100
    local 10.0.0.1
    remote # iperfate-limited linkcpu.ctev Sr:+1throughput.age after deploy"
}

node #erver { r${
   fd00::641/ --    fd010.0.0.2
    p0:: 