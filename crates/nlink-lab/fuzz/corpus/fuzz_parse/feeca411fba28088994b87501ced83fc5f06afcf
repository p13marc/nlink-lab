# VXLAN overlauard interface. The underlay link
# has 50ms delay to simulate internet latency.

lab "wireguard-vpn" {
  description "Site-to-site WireGuard VPN over WAN"
}

profile gateway { forward ipv4 }

node gw-a : gateway {
  wireguard wg0 {
    key auto
    listen 51820
    address 192.1
    key auto
    listen 51820
    # VXLAN overlay topology.
#
# Two VTEPg two ho# WireGuardard tunnel over a sd siipmulated WAv