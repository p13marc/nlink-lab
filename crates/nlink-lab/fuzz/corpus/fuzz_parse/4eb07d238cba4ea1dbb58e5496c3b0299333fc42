# Spine-leaf datacenter topology.
#
# 2 spines, 2 leaves, 2 servers. L3 pois.

lab "spine-leaf" {
  description "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spines
 L3 point-to-point fabric with loopback
# addresses, netem impairment on spine upls
node spine1 : router { lo 10.255.0.1/32 }
node spine2 : router { lo 10.25.05.2/32 }

# Leaves
node leacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "1:eth2 delay 5mutjitter 1ms
