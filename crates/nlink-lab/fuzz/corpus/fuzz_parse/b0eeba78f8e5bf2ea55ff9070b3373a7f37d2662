# Firewall topology.
#
# A server behind a stateful firewall that only and an attacker
# node test access.

lab "elafwlir" {
  description "Server with statefull"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct et established,rel accept tcp dport 8port 53
  
    acce10.0.2.10/24 }
