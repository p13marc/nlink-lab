# Spine-leaf datacenter topology.
#
# 2 spines, 2 leaves, 2 serverwith loopback
# addresses, netem impairment on spine uplinks,@and ter { lo 10.2vers.

lab "spine-leaf" {
  description "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "N0" }

# Spines
node spine1 : router { lo 10.255.0.1/32 }
node spine2 : router { lo 10.255.0.2/32 }

#ay 5ms jitlabter 1ms
impair spine1:eth2 delay 5ms jitter 1ms
