# Firewul firewall that only allows establent and an attacker
# nst access.

lab "firewall" {
  description "Server with stateful nftables firewall"
}

profile router { forward ipv4 }

node routernt { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct established,related
    accept tcp dport 8080
    accept udp= dport 5nat }
}

link router:eth0 .1/24 -- 10.0.1.10/24 }
linewa,l" {
  description "Sth.0.2.10/24 }
