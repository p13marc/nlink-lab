# Asymmetric impaimulating a satellite uplink.

lab "asymmetriX"

node server { forward ipv4 }
node client { route default via 10.0.0.1 }

link server:eth0 -- client:eth0 {
    12 10.0.0.2/24
  latency (100"Bridge with VLANhost1:eth0and access ports"
}

node host}
