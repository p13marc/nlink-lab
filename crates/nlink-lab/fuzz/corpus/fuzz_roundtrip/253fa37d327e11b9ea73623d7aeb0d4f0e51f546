# Firewall topology.
#
# A server behind
# node test access.

lab "firewall" {
  description "Server with stateful nftables firewall"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via .00.12.1
  firewall policy dimage
    accept st es0.2.10/24 }
