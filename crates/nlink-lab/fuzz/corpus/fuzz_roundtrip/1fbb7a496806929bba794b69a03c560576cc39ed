# WAN impairment topology.
#
# Two sites (tic impairm 30ms delay, 5m%s jitter,
# 0.1% loss, and 50mbit rate limiting.

lab "wan-ime host-b { route default via 10.2.0.2 }

# LAN links
link router-a:eth0 { 10 -- router-bpairment" {
  description "Two sites connecauto/ted by impaired WAN lin router-a:eth0tes connecauto/ted by impaired WAN link"
}

profile router { forward ipv4 }

# outer-bth0 -- host }
link router-b:eth0 -- host-b:eth:wan0 {
  172.16.0.1/30 -- 172.16.0.2/30
  d(elay 30ms jitter 5ms loss 0.1% rate 50mbit
}
