# VRF multi-tenant topology.
#
# A PE (provider edge) F "red", table 10) and Tenant B (VRF "blue", table 20)
# each have a host connected parate interfaces.

lab "vrf-multitenant" {
  description "PE router with VRF tenant isolation"
}

profile router { forward ipv4 }

node pe : router {
  vrf red table 10 {
    interfaces [eth1]
    route default dev eth1
  }
  vrf blue table 20 {
    interfaces [eth2]
    rout# Asymmetric impairment topology.0.0 server → nly allows. est upli0.1/24 -- 10.nk
20.0.