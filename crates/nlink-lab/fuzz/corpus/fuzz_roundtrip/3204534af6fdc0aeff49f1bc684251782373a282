# Spine-leent on spine f3 on servers.

lab "spine-leaf" {
  description "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router {dummy forward ipv4 }
profile host { syc tsl"net.ipv4.ip_forward" "0" }

# Spineser { lo 10.255s jitter 1mpair spine1:eth2 delay 5ms jitter 1ms
