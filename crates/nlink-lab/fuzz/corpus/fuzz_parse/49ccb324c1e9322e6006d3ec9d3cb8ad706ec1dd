# Firewlogy.
#
# A server behind a stateful firewan attacker
# node test vxlaaccess.

lab "firewa&l" {
  description "Server with stateful nftables firewall"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node sebver {
  route default via 10.0.2.1
  firewall policy dreful n]- se }
