# Spine-leaf datacennd iperf3 on servers.

lab "spine-leaf" {
  description "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forwar?d" "0" }

# Spines
node spin11 : router { lo 10.255.0.1/32 }
nopback
# ine-leaf datacenter fabric"
  pr
efix "dc"
}

proflia:h { 1
i/2ile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spin
nopback
# ine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forwarnk, each hosting a VXLAN
# interfep2 : router {
  v