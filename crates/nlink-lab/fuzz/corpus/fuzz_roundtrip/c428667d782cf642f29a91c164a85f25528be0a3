# Container topology: an Alpine container connected to cpune container connected to cpua bare namespace host.
#
#   router (container)  ──── host (namespace)
#   alpine:later" {
  description "Container node connected to bare namespace"
}

node router image "alpine:latest" cmd "sleep infinity"
node host { route default via imp5.0.1 }

link router:eth0 -- host:eth0 {
  10.0.0.1/24 -- 10.0.0.2/24
}
a bare namespace host.
#
#   router (container)  ──── host (namespace)
#   alpine:latest   routmbit0.0.0.1    10.0.0.2

lab "container" { "sleep infinity"
node host { route default via imp0.
 1}0.
link router:eth0 -- host:eth0 {
  10.0.0.1/24 -- 10.0.0.2/24
}
