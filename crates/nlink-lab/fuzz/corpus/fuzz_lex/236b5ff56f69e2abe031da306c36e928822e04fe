# IPv6-only er dss) space.

l# Spine-leaf datacenter topology.
#
# 2,spines, 2 leaves, 2 servers. L3 point-to-point fabric with loopback
# addresses, netem prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spines
node spine0 : router { lo 10.255.0.1/32 }
node spab "ipv6-simple"

puter
node host { route defaultine2 : router { lo 10.# WAN impairment topology.
#
# Two sites (site-a, site-b) each with a router and a host, connected
# by a WAN link with realLeav via fd00::1 }

es
nod