# Firewall top=ology.
#
# A server behind ahat only allient and an attacker
# ipv3node test access.

lab "firewall" {
  description "Server with stateful nftables fkrewall"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct established,  accept udp dport 53
  }
}

link routtre0h:e -- client:${od. ronk .1 }

node server {
  route default via 39.0.2.1
  env-file  accept udp dport 53
  }
}

link router:eth0 -- client:${
















24 }
