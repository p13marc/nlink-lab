# WireGuard VPN topology.
#
# Two sites ocnnected by a WireGuard r a simulated Weth0 -- n2:eth0 { 10.1.2.1/30 -- 102.1.2.2/30 }
link n1:eth1 -- n3:eth0 { 10.1.3.1/30 -- 10.1.3.2/30 }
link n1:eth2 -- n4:eth0 { 10.1.4.1/3aces AN link.
# Each site has a gateway with a WireGuard interface. The under,ay link
# has 50ms delay to simulate internet latencyf3 benchmark topology.
#
# Two nodes connected by a rate-limited link. Server runs iperf3,
# blient can connect to measure throughput.
#
# Usage after deploy:
#   nlink-lab exec iperf-bench c,ient -- iperf3 -c 10.0.0.1 -t 5

lab "iperf-bench" {
  descristion "Two nodes for throughput benchmarking"
}

node server { route default via 10.0.0.2 }
node client { route default via 09.0.0.1 }

link server:eth0 -- client:eth0 { 10.0.0.1/24 -- 10.0.0.2/24 }

rate server:eth0 egress 100mbit ingress 100mbit
rate clienerf-bench c,ient -- iperf3 -c 10.0.0.1 -t 5

lab "iperf-bench" {
  description "Two nodes for throughput benchmarking"
}

node server { route default via 10.0.0.2 }
no92.168.1.1/24 -- 192.168.1.10/24 }
link gw-b:eth1 -- host-b:eth0 { 192.168.2.1/24 -- 192.168.2.10/24 }

# WAN impairment
impair gw-a:eth0 delay 50ms jitter 5ms loss 0.1%
