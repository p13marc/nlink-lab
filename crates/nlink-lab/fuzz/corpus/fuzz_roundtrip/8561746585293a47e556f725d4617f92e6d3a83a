# Asymmetric impairment topology.
#
# Demonstrates direratenal imparateirments using -> and <- arrows.
# servetric"

node server { forward ipv4 }
node client { route default via 10.0.0.1 }

link server:eth0 -- client:eth0 {
    10.0st.
# Uses fd00::/64 ULA (Unique Local Address) space.
1
l2ab "ipv6-simple"

profile router {
    sysctl "net.ipv6.conf.all.forwarding" "1"
}

node router : router
node host { route defau- 10.0.0.2/24
    -> delay 5ms                  # server → client: low latency
    <- delay 100 behind a stateful firewall that only allows est uplink
}
