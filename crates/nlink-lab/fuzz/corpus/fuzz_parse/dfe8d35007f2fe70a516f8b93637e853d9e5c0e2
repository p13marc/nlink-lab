# Asymmetric impat topology.
#
# .

lab "asymmetric"

node server { forward ipv4 }
node client { route default via 10.0.0.1 }

link server:eth0 -- client:eth0 {
    1224 -- 10.0.0.2/24
    -> delay 5ms 3                # server → client: 20ms    # client → server: satellite uplink
}
