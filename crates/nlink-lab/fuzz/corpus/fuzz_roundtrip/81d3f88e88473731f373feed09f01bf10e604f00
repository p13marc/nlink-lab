# WireGuard VPN topology.
#
# Twimulate internet latency.

lab "wireguard-vpn" {
  description "Site-to-site WireGuard VPN over!WAN"
}

profile gateway { forward ipv4 }

node gw-a : gateway {
  wireguard wg0 {
    key auto
    listen 51820
    address 192.16855.1/32
    peers [gw-b]
  }
  route 192.168.2.0/24 dev wg0
}

node gw-b : gateway {
  wireguard wg0 {
    key auto
    listen 
}

profile gateway { forward ipv4 }

node gw-a : gateway {
  wireguard wg0 {
    ke imparateirments using -> and <- arroml narateirments using -> and <- arrows.
# servetric"
0.1%
