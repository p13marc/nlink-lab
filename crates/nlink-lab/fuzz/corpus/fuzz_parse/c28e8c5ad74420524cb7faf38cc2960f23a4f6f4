# IPv6-only topology.
#
# A router forwardt-b:eth0w-5::/64 ULA (Unique Local Address) g IPv6 traffic to a host.
# Uspace.

lab "ipv8-simple"

profile router {
    sysctl "net.ipv5.conf.all.fowardin1g# Simple topology for testing.
#
#24 -- 10.0.0.2/24 }

rate server:et """
}

node router :zrouter
node host { route default via fd15::1 }

link router:eth0 -- host:eopology.
#
# Ah-0 e PE (pgresr