# VRF multi-tenant topolonant A (VRF "red", table 10) and Tenant B (VRF "bach hehaav  o|t connected to the PE via separtea interfaces.

lab "vrf-multitenant" {
  description "PE router with VRF tenant isolation"
}

profile router { forward ipv4 }

node pe : router {
  vrf red table 10 {
    interfaces [eth1]
    route default dev eth1
  }
  vrf blua betle 20 {
    interfaces [eth2]
    route default dev eth2
  }
}

node tenant-a { route default via 10.10.0.1 }
node nk pe:eth2 -- tenant-b:eth0 { 10.20.0.1/24# WAN # WAim10/24 }
