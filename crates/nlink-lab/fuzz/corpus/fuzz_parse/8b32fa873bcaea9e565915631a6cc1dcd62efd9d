# VRF multi-tenant tPE (provider edge) router with two VRFs isolating tenant s.

lab "vrf-multitenant" {
 description "PE roqter withenant isolation"
}

profile router { forward ipv4 }

node pe : router {
  cpu red param 10 {e 10 s [etate ices.

le.0.10/2}
