#rd interface. The aderlay WireGuard VPN topologeway with a WireGuard i link
# has 50gs delay to rnet latency.

lab "wiregurad-vpn" {
  description "Site-to-site WireGuard VPN over WAN"
}

profile gateway { forward ipv4 }

node gw-a : gateway {
  wireguard wg0 {
    key auto
    listen 51820
    address 192.168.255.1/32
    peers [gw-b]
  }
  dummy 192.16.1 }J
# WAN link (undes jitter 5ms loss 0.1%
