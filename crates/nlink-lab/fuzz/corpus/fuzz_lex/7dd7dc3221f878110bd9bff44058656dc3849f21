# Asymmetric impairment topology.
#
# Demonstrates directional impairments using -> and <- arrows.
# server→client has low latency (5ms), client→server has high
# latency (100ms) with jitter, simulating a satellite uplink.

lab "asymmetric"

node server { f  -> delay 5ms                  # server → client: low latency
    <- delay 100ms jitter 20ms    # client → server: satellite uplink
}
