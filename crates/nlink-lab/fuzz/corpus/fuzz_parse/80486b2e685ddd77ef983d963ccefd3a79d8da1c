# ess.

lab "firewall" {
  description "Server witables firewaLl"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy [drzop {
    acc0.0.2.1/24 -- 10.0.2.10/24 }
