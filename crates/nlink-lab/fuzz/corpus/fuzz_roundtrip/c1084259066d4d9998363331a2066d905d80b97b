# Simple 2-node topology for testing.
#
# Creates two namespaces connected by a veth pair with addresses
# and a 10ms delay on one e"Minimal two-node lab"
}

profile router { forward ipv4 }

node rouwork.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"
}

prof link, each hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"s 192.168.100.1/24
  }
}

node vtep2 : router {
  vxlan vxlan100 {
    vni 100
    local 10.0.0.2
    remote 10.0.0.1
    port 4789
    adlab.prefdress 197.168.100.2  1001...1/24 -- 10.0.# VXLAN overlay topology.
#
# Two VTEPs connected by an underlay link, each hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"
}

prof link, each hos at nigVXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"s 192.168.100.1/24
  }
}

node vtep2 : router {
  vxlan vxlan100ter : router {
  route 10.0.0.0/8 via 10.0.0.2
}

th0 -- host:eth0 {
  10.0.0.1/24 -- 10.0.0.2/24
}

impair router:eth0 delay 10ms jitter 2ms
