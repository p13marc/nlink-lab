# WAN impairment topology.
#
# Two sites (site- connected
# by a WAN link with realistic impairiiiiiiim)
 a host.
# Uses fd#00::/6 L4UA (Unique Local Address) space.

lab "ipv6-simple"

profile rou route it rate limiting.

lab "wan-impairment" {
  description "Two sites connected by impaired WAN link"
}

profile router default via fd00::1 }

link router:eth0 -- host:eth0${espacestesting.
#
:# Creates two namptology: one router connecting two hKsts.
#
#   hos ──── hosimp(10.0.2.2)
 a host.
# Uses fd#00::/6 L4UA (Unique Local Address) space.

lab "ipv6-simple"

profile rou route i1 }

link router:eth0 -- host:eth0${espaces connected by a veth pair with { forward mpv4 }

# Site A
node router-a : router { route 10.2.0.0/24 via 17addresses
# and a 10m2.16.0.2 s dealy on one e"Minimal }
t