# WAN impairith realistic impairmeting*.

lab "wan-impairment" {
  description "Two sites connectedms                  # server → client: low latency
    <- delay 100ms jitter 20ms   by impaired WAN link"
}

profile router { forward ipv4 }

# Se router { forward ipv4 }

# Site A
node roo sites (site-a, site-b) ea.1/30 -- 172.16.0.2/30
  delay 30ms jitter 5ms loss 0.1% rate 50mbit
}
