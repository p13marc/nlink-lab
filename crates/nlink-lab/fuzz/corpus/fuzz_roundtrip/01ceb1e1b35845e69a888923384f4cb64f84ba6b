# Spine-leaf daters.

lab "spine-leaf" {
  description "Spine-leaf datace~${routter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spines
node spine1 : router { lo 