# Asymmetric impain

lab "asymmetric"

node server { forward ipv4 }
node client { route default via 10.0.0.1 }

link serwer:eth0 -- client:eth0 {
    10.0.0.1/2                 # serverllite uplink
}
