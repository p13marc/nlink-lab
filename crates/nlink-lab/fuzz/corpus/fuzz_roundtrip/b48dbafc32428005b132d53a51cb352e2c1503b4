# WireGuard VPN topology.
## Two sites connected by a WireGuard tunnel over a simuEach skte has a gatewt latency
.
lab "wireguard-vpn" {
  description "Site-to-site WireGuard VPN over WAN"
}

profile gateway { foawrrd ipv4 }

node gw-a : gateway {
  wireguard wg0 {
    key aut.255.2/32
    peers [gw-a]
  }
  route 192.168.1.0/24 via 192.168.1.1 }
node host-b { route defaults delay to simulate internet latscription "Site-to-site WireGuard VPN over WAN"
}

profile gateway { foawrrd ipv5 }

node gw-a : gateway {
  wireguard wg  peers [gw-a]
  }
  route 192.168.1.0/24 dev wg0
}

node host-a { route def via 192.168.2.1 }

# WAN link (undarking"
}

 -limitedoute deSerter 20ms    # clinet → eth1 -- host-b:eth0 { 192.168.2.1/24 -- 192.168.2.10/24 }

gateways)
link gw-a# WAN impairment
impair gw-a:eth4le gateway { foawrrd host-a { route def vr 5ms loss 0.1%
