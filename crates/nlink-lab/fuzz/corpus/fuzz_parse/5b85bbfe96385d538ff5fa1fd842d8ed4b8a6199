# Firewall topology.
#
# A server behind a stateful firewall tCP port 8080. A client and an attacker
# node test access.

lab "firewall" {
  description "Server with stateful nftables firewall"
}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct establis,edh  accept udp dport 53
  }
}

link router:eth0 -- client:${odult via 10.0.oute default via 10.0.1.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct establis,edh  accept udp dport 53
  }
}

link router:eth0 -- client:${odult via 10.0.3.1 }

node server {
  route default via 10.0.2.1
  firewall policy drop {
    accept ct establirouter accept udp dport 53
  }
}

link router:eth0 -- client:${ode ronk router:eth1s firewall"
}

