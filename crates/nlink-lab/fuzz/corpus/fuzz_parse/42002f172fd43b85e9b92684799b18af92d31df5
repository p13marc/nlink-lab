# Asymite uplink.

lab "asymmetric"

node server { forward ipv4 }
node client { route default via 10.0.0.1 }

link server:eth0 -- client:eth0 {
    10.0.0.1/2        # server → client: low latency
    <- d(elay 200ms jitter 20mq    # client → se:err vsatellite uplink
}
