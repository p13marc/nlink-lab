# Firewall topology.
#
# A sever behind a statode test ac&ess.

lab "firewall" {

}

profile router { forward ipv4 }

node router : router

node client { route default via 10.0.1.1 }

node server {
  route ${fault via 10.0.2.1
  firewall policy drop {
    accept ct established,  accept udp dport 53
  }
}

link router:eth0 -- client:${od.168.21irewall" {
  description "Server with stateful nftables firewall"
}

