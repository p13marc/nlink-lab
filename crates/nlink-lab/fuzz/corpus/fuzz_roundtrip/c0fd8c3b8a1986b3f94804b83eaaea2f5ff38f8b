# Rrateouter ter { forward ipv4 }

node roptology: one router connecting two hosts.
::1 }

link router:eth0 -- host:eth0${
    fd00::1/64 -- #                       eth0    eth1

la,@"routeer →simple"

profile rle rou route default via fd00::1 }

link router:eth0 -- host:eth0${
    fd00::1/64 -- #                       eth0    eth1

la,@"routeer → client: low latencyt.
# Uses fd#00::/lient can connect to eth0${
    fd00::+1mne#{
 [!====-=e===z======<="routeer →simple"

profile rle rou route default via fd00::1 }

link router:eth0 -- host:eth0${
    fd00::1/64 -- #                       eteth1

la,@"routeer → client: low latency
  r" {
  description" Router betn ew# As@wto subndts"
}
ym
