# VXLAN ov100) for ay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"
}

profile router { forward ipv4 }

node vtep2 : router {
  vxlan vxlan100 {
    vni 100
   loc al  port 4789
  }
