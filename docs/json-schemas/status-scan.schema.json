{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "nlink-lab status --scan --json",
  "description": "`status --scan --json` envelope.",
  "type": "object",
  "properties": {
    "labs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/LabInfo"
      }
    },
    "orphans": {
      "$ref": "#/$defs/Orphans"
    }
  },
  "required": [
    "labs",
    "orphans"
  ],
  "$defs": {
    "LabInfo": {
      "description": "Summary info about a running lab (for status listing).",
      "type": "object",
      "properties": {
        "created_at": {
          "description": "ISO 8601 creation timestamp.",
          "type": "string"
        },
        "name": {
          "description": "Lab name.",
          "type": "string"
        },
        "node_count": {
          "description": "Number of nodes.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "name",
        "node_count",
        "created_at"
      ]
    },
    "Orphans": {
      "description": "Resources on the host that look like lab-owned state but have no matching\n`state.json` — usually left behind by a crashed deploy.",
      "type": "object",
      "properties": {
        "bridges": {
          "description": "Root-namespace mgmt bridges (`nl{hash8}`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "netns": {
          "description": "Named network namespaces tagged by nlink-lab (`netns_tag`) whose\nowning lab is not registered, or is registered but no longer\nclaims them in `state.json`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "stale": {
          "description": "Labs whose state file claims namespaces that no longer exist on the\nhost — the mirror case of the above (state with no resources). Most\ncommonly caused by a reboot.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/StaleLab"
          }
        },
        "untagged_ignored": {
          "description": "Namespaces on the host without an nlink-lab ownership tag. Never\nlisted and never touched (#29 — they belong to libvirt, podman,\nCNI, ...); surfaced only as a count for `status --scan -v`.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "veths": {
          "description": "Root-namespace mgmt veth peers (`nm{hash8}{idx}`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "bridges",
        "veths",
        "netns",
        "untagged_ignored",
        "stale"
      ]
    },
    "StaleLab": {
      "description": "A state-backed lab with one or more namespaces missing from the host.",
      "type": "object",
      "properties": {
        "missing_namespaces": {
          "description": "Namespaces claimed by `state.json` that are absent from `ip netns list`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "name": {
          "description": "Lab name.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "missing_namespaces"
      ]
    }
  }
}
