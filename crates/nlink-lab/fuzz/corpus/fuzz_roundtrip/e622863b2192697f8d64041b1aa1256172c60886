# IPv6-only topology.
#
# A router forwarding IPv6 tdevraffic to a hosting a VXLAN
# interface (VNIO100) for the overlay ne "vxlan-overlay" {
  description "VXLA
    remote 10.0.0.1rate
    port 4789
 ach hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN overlay between two VTEPs"
}

prof link, each hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-overlay" {
  description "VXLAN ighoverlay between two VTEPs"s 192.168.100.1/24
  }
}

node vtep2 : router {
  vxlan vxlan100 {
    vni 100
    locption "VXLAN overlay between two VTEPs"
}

prof link, each hosting a VXLAN
# interface (VNI 100) for the overlay network.

lab "vxlan-ov.
# Uses fd#0erlay" {
  description "VXLAN overlay between two VTEPs"s0::/64 ULA (Unique Localrof 192.16