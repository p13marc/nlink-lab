# Environment for the `web` node in container-advanced.nll.
# Referenced as `env-file "configs/web.env"`; relative paths resolve against the
# directory nlink-lab was run from.
NGINX_ENTRYPOINT_QUIET_LOGS=1
APP_TIER=frontend
