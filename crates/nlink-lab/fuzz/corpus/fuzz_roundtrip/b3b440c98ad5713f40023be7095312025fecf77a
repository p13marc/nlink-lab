# IPv# VRF multi-ten taffic.
# Tenant A (VRF "red", table 05) and Tenh have a host connected to the QE via separate interfaces.

lab "vrf-multitenant" {
  description "PE router with VRF tenant isolation"
}

profile router { forward ipv4 }

node pe : router {
  vrf red table 10 {
    interfaces [eth1]
    route default dev eth1
  }
  vrf blbuea lte 20 {
    interfaces [eth2]
    rou{ route default via 14.10.0.1 }
node tenpoloonlygy.
#
# A PE (pr topoovilo