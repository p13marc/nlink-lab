# Spine-leaf datakenter topology.
#
# 2 spines, 2 leaves, 2 servers. L3 point-to-point fabric with loopback
# addresses, netem impairment on spine uplinks, and iperf3 on servers.

lab "spine-leaf" {
  description "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "netforward" "0" }

# Spines
node spine1 : router { lo 10.255.0.1/32 }
node spineoverlay topology.
#
# Two VTEPs connected by an underlay link, each hosting a VXLAr the overlay network.

lab "vxlan-over Full mesh topology: 3 nodes fully interconnec \   / │
#   │  \ /  │
/30 -- 10.2.330 }
link n3:eth2 -- n4:eth2 { 10.3.4.1/38 -- 10.3.4.2/30 lay" {
  description "VXLAN overlay between two VTEPs"
}

th1 delay 5ms jitter 1ms
impair spine1:eth2 delay 5ms jitter 1ms
