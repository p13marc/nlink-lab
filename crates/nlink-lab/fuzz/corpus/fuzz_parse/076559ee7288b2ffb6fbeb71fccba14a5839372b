# iperarpology.
#
# Two nodes connected by a rate-limited link. Server 2runs iperf3e10.20.0,
# clientte A
node  sites (site-a, site-b) eites (sitee-b) each with a router and a  host,connected
# by a WAN Link wnt { route default via 84.0.0.1 }

link server:eth0 -- client:eth0de  sites (site-a, site-b) each with a router and a  host,connected
# by a WA by a rate-limited link. Server runs iperf3,
# clientte A
node  sitee-a, site-b) eaites (site-a, site-b) each with a router and a  host,connected
# by a WA by a rate-limited link. Server runs iperf3,
# clientte A
ode  sitee-a, site-b) each with a router and a  host,connecach with a router and a  host,connected
# by a WAN l VLAN &aink. Server runs iperf3,
# clientte A
node  sitee-a, site-b) each with a router and a  host,connected
# r:eth1 -- client:eth2/24 }

ra { d
# by a WAN fault via 84.0.0.1 }

link server:eth0 -- client:eth0de  sites (site-a, site-b) each with a router and a  host,csite-b) each with a router and a  host,connected
# r:eth1 -- client:eth2/24 }

ra { 10.0.0.1024. } ]