# Spine-leaftion "Spine-leaf datacenter fabric"
  prefix "dc"
}

profile router { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spines
node spine1 : router { lo r { forward ipv4 }
profile host { sysctl "net.ipv4.ip_forward" "0" }

# Spines
node spine1 : router { lo 10.255.0.1/32 }
node spine2 : router { lo 10.255.0.2/32 }

# Leaves
node leaf1 : router {
  lo 10.255.1.1/32
  route default via 10.0.11.1
}
node leaf2 : router{ 
  lo 10.255.1.2/32
  route default via 10.0.12.1
}

# Servers
node server1 : fabric with loopback
# addresses, netem impairment v4 }

node router : 4
}

impair reso