# nlink-lab cookbook

Hands-on recipes that pair runnable NLL with a writeup explaining
when, why, and how. Each recipe is self-contained — copy the NLL,
run the commands, see the result.

## Highlighted

These recipes show off capabilities that are awkward, expensive, or
structurally impossible in container-based labs.

- [**Distance-dependent satellite mesh**](satellite-mesh.md) — 12
  satellites in a ring with three path classes, generated by a `for`
  loop with modulo arithmetic. Per-pair netem on a shared L2 — the
  thing Docker's network model can't express.

## Networking primitives

- [**VRF multi-tenant separation**](vrf-multitenant.md) — two
  customers on one PE router, isolated at L3 via VRFs (red/blue
  tables). Overlapping address space allowed.
- [**WireGuard with auto-key generation**](wireguard-mesh.md) —
  site-to-site VPN over a 50ms WAN underlay; keys generated at
  deploy time, no manual key dance.

## Coming soon

The following recipes ship over the next few releases:

- macvlan attach to a host physical NIC
- Stateful nftables firewall with conntrack zones
- Bridge VLAN trunks (802.1Q access/trunk port mix)
- iperf3 benchmark with hard assertions on p99 latency
- Healthcheck-gated container startup with `depends_on`
- CI matrix: `--set` parameter sweeps from a shell loop
- Parametric `import for_each` for spine-leaf fabrics
- Multi-tenant VRF + WireGuard + nftables WAN
- Mid-test partition via the scenario engine
- Asymmetric WAN with a one-way loss spike

In the meantime, every example under [`../../examples/`](../../examples/)
is a runnable demonstration of one or more features.

## How to read a recipe

Each recipe starts with **when to use this** and **why nlink-lab is the
right tool**. If you can't answer "yes" to both, the recipe isn't for
you — most NLL feature questions answer faster against
[`docs/NLL_DSL_DESIGN.md`](../NLL_DSL_DESIGN.md).

## Contributing a recipe

Pick a real workflow you've solved with nlink-lab. Write up:

1. The NLL (under `examples/cookbook/`).
2. The writeup (under `docs/cookbook/`).
3. A note in the writeup of when this is the *wrong* tool.

A passing parse + validate is the minimum gate; the
`test_all_nll_examples_parse` test in
`crates/nlink-lab/src/parser/nll/lower.rs` enforces this for any
NLL file under `examples/cookbook/`.
