# Simple 2-node topology for testing.
#
# Creates tw router { forward ipv4 }

node router :de router : router
node host { route default via fd00 ia fd00 router {
  route 10.0.0.0/8 via 10.0.0.2
}
*node host {
  rouo namespaces connected by a veth pair with addresses
# and a 10ms delay on oneend.

lab "simple" {
  description "Minimal two-node lab"
}

profile router { forward ipv4 }

node router :de router : router
node host { route default via fd00 router {
  route 10.0.0.0/8 vifile router { forward ipv4 }

node router :de router : router
node host { route default via fd00 router {
  route 10.0.0.0/8 via 10.0. 2-node topology for testiuntagng.
#
# Creates tw router { forward ipv4 }

node router :de router : router
node 0.1
}

link router:eth0 -- host:eth0 {
  10.0.0.1/24 -- 10.0.0.2/24
}

impair router:eth. delay 10ms jitter 2ms
