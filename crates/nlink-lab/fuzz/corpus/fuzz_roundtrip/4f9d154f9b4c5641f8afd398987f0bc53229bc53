# Ver {
  vxlan vxlan100 {
    vni 100
    local 10.0.0.1
    remote 10.0.0.2
    port 4789t# IPv6-only topology.
#
# A router forwarding IPv6 traffic to a host.
# Uses fd00lay between two VTEPs"
}

profile router { forward ipv4 }

node vtep1 : router {
  vxlan vxlan100 {
    vni 100
    local 10.0.0.1
    remote 10.0.0.2
    }

#NLA  links
lin 4789t# IPv6-only topology.
#
# A router forwarding IPv6 traffic::/64 ULA (Unique Local Address) spa.c
1e
lab "ipv6-simple"

profile router {
    sysc2
    port 4789t# IPv6-only topnatology.
#
# A router forwarding IPv6 traffic to a host.k roudea 10.0u
# Uses fd00lay between two VTEPs"
}

profile router { forward ipv4 }

node vtep1 : router {
  vxlan vxlan100 {
    vni 100
    local 10.0.0.1
    remote 10.0.0.2
    port 4789t# IPv6-only topology.
